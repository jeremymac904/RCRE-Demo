'use client'
import {useEffect,useState} from 'react'
const input='block w-full border border-hair bg-ink p-2 mt-2'
export function InvitationManager(){const [mail,setMail]=useState<any[]>([]),[error,setError]=useState('');const load=()=>fetch('/api/platform/mail').then(r=>r.json()).then(setMail);useEffect(()=>{void load()},[]);return <section className="mt-10"><h2 className="font-display text-2xl">Invite a synthetic teammate</h2><form className="grid sm:grid-cols-2 gap-4 my-5" onSubmit={async e=>{e.preventDefault();setError('');const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/platform/invitations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)setError(d.error);else void load()}}><label>Name<input className={input} name="name" required/></label><label>Synthetic email<input className={input} name="email" type="email" required placeholder="teammate@example.test"/></label><label>Office<select className={input} name="officeId"><option value="fl">Florida</option><option value="al">Alabama</option></select></label><label>Role<select className={input} name="role">{['agent','team_leader','managing_broker','transaction_coordinator','marketing_admin','trainer'].map(r=><option key={r}>{r}</option>)}</select></label><button className="border border-hair p-3 self-end">Create one-use invitation</button></form>{error&&<p role="alert">{error}</p>}<h3 className="font-display text-xl">Local mail capture</h3><p className="text-sm text-chalk-muted my-3">These messages are stored locally and never sent. Each invitation or recovery link expires and can be used once.</p>{Array.isArray(mail)&&mail.slice().reverse().map(m=><div className="border-b border-hair py-4" key={m.id}><p>{m.subject} · {m.recipient}</p><p className="text-sm text-chalk-muted">{m.status}</p><a className="text-brass-ink" href={m.url}>Open captured access link →</a></div>)}</section>}
