import {requireActor,AccessError} from '@/lib/platform/auth'
import {assetBytes,archiveAsset} from '@/lib/platform/library'
export const dynamic='force-dynamic'
export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){try{const {asset,bytes}=assetBytes(await requireActor(),(await params).id);const preview=new URL(req.url).searchParams.get('preview')==='1'&&['image/png','image/jpeg','video/mp4'].includes(asset.mime);const headers:Record<string,string>={'Content-Type':asset.mime,'Content-Disposition':`${preview?'inline':'attachment'}; filename*=UTF-8''${encodeURIComponent(asset.name)}`,'X-Content-Type-Options':'nosniff','Cache-Control':'private, no-store','Content-Security-Policy':"sandbox; default-src 'none'",'Accept-Ranges':'bytes'};const range=req.headers.get('range');if(range){const match=range.match(/^bytes=(\d+)-(\d*)$/);if(!match)return new Response(null,{status:416,headers:{'Content-Range':`bytes */${bytes.length}`}});const start=Number(match[1]),end=match[2]?Math.min(Number(match[2]),bytes.length-1):bytes.length-1;if(start>end||start>=bytes.length)return new Response(null,{status:416,headers:{'Content-Range':`bytes */${bytes.length}`}});headers['Content-Range']=`bytes ${start}-${end}/${bytes.length}`;headers['Content-Length']=String(end-start+1);return new Response(new Uint8Array(bytes.subarray(start,end+1)),{status:206,headers})}headers['Content-Length']=String(bytes.length);return new Response(new Uint8Array(bytes),{headers})}catch(e){return fail(e)}}
export async function DELETE(req:Request,{params}:{params:Promise<{id:string}>}){try{const origin=req.headers.get('origin');if(origin&&new URL(origin).host!==new URL(req.url).host)throw new AccessError('Origin mismatch');return Response.json(archiveAsset(await requireActor(),(await params).id))}catch(e){return fail(e)}}
function fail(e:unknown){return Response.json({error:e instanceof Error?e.message:'Request failed'},{status:e instanceof AccessError?e.status:500})}
