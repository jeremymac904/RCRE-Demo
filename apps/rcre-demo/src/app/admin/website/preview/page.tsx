import {redirect,notFound} from 'next/navigation'
import {requireActor,can} from '@/lib/platform/auth'
import {getRecord} from '@/lib/platform/store'
import type {PublicContent} from '@/lib/public/content'
import {PublicShell,PublicHero} from '@/components/public/PublicShell'
export const dynamic='force-dynamic'
export const metadata={title:'Private content preview | RCRE',robots:{index:false,follow:false}}
export default async function Preview({searchParams}:{searchParams:Promise<{path?:string}>}){let actor;try{actor=await requireActor()}catch{redirect('/login')}if(!can(actor,'cms'))redirect('/forbidden');const {path}=await searchParams;const record=path?getRecord<PublicContent>('public_content',path):null;if(!record||record.organizationId&&record.organizationId!==actor.organizationId)notFound();return <PublicShell><div className="public-wrap"><p className="public-notice">Private saved draft preview · Revision {record.revision} · Not publicly published</p></div><section className="public-wrap public-content"><h2>Saved publication settings</h2><dl><dt>SEO title</dt><dd>{record.seoTitle||record.title}</dd><dt>Search and social description</dt><dd>{record.description||'No description supplied'}</dd><dt>Canonical URL</dt><dd>{record.canonical||'https://rcregroup.com'+record.id}</dd><dt>Indexing when published</dt><dd>{record.noIndex?'Excluded from search and sitemap':'Public indexing allowed'}</dd><dt>Permanent redirect when published</dt><dd>{record.redirectTo||'No redirect'}</dd><dt>Navigation</dt><dd>{record.navigation?.visible?`${record.navigation.label} · Order ${record.navigation.order}`:'No additional navigation entry'}</dd><dt>Image</dt><dd>{record.image?.src?`${record.image.src} · Alternative text: ${record.image.alt}`:'No selected image'}</dd></dl>{record.profile&&<><h2>Public profile details</h2><dl><dt>Phone</dt><dd>{record.profile.phone}</dd><dt>Email and intended inquiry recipient</dt><dd>{record.profile.email}</dd><dt>License</dt><dd>{record.profile.license}</dd><dt>Market</dt><dd>{record.profile.market}</dd></dl></>}{record.schema&&<><h2>Visible article attribution</h2><p>{record.schema.authorName||'RCRE editorial desk'}{record.schema.datePublished?' · '+record.schema.datePublished:''}</p><p>Structured data: Article and BreadcrumbList.</p></>}</section><PublicHero title={record.title} description={record.description}/><div className="public-wrap public-content public-prose">{record.image?.src&&<img src={record.image.src} alt={record.image.alt} style={{maxHeight:360,marginBottom:30}}/>}{record.body.split('\n\n').map((p,i)=><p key={i}>{p}</p>)}</div></PublicShell>}
