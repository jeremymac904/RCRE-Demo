# RCRE implementation register

Local review register, September 10, 2026. Final production app is running on loopback3200; editing agents stopped. Canonical app: `apps/rcre-demo`; persistent local store: `runtime/data/platform.sqlite`. Historical `apps/rcre` is not a separate running product. Status is per verified behavior, not an assertion that the full mandate is finished.

Status keys: **1** implemented and verified locally; **2** authorized sandbox verified; **3** explicit simulation with durable state transition; **4** named external dependency; **5** not implemented or not yet verified (explicitly identified below).

| Requirement | Routes / roles | Source and persistent actions | Errors / evidence / result | Activation dependency |
|---|---|---|---|---|
| PUB-01–15 | `/`, public catch-all, `/team`, profiles, markets, guides, `/blog`, legal, contact, search, properties; visitors | Authorized source inventory and original copy; private visitor favorites/searches; inquiry records; published CMS revisions | **1/3** public HTTP 9 checks, CMS 10 checks; public browser families at375; missing pages404, malformed400, consent false retained, anonymous visitor isolation | Current sitemap403; inaccessible source content, photo rights and factual/legal review remain **4** |
| LEND-01–09 | `/financing`; visitors; owner lender settings | Exact mandate contact/NMLS/choice block, educational calculator, verified external application link, local recipient/referrer/consent inquiry | **1/3** calculator12 tests; CUA contact submission; payment boundary cases; mobile and desktop both themes | Jeremy portrait/brand rights, endorsement/commercial review, state eligibility before state promises **4** |
| ID-01 | `/login`, `/access/[token]`, `/recovery`, `/onboarding`, sessions; seven roles | Opaque revocable local sessions, one-use invitations/recovery, local mail capture, membership enforcement | **1/3** role/API denial checks. one-use invite/recovery lifecycle and8 access/policy browser checks pass; restart preserves the opaque session | Real identity provider, outbound mail **4** |
| CRM-01 / TODAY-01 / PIPE-01 | `/today`, `/crm`, detail, `/pipeline`; own agent/office leadership/owner | Canonical contacts, assignment audit, notes/call attempts vs connected, optimistic edit, tasks, saved views | **1/3** platform unit+browser: contact/task refresh, unrelated agent404, stale409 | FUB real source activation **4**; per-user list filters/page retained across refresh; browser navigation check passed. FUB-owned edits are durable proposals; tombstoned sources stay archived |
| CAL-01 | `/calendar`, Today day planner; CRM roles and marketer | Local appointments, conflict checks, day/week, saved workhours/timezone, proposed blocks fit tasks around commitments | **1/3** day-plan tests; browser block persists. calendar reminder preference, quiet suppression, deduplication and source links tested; automatic worker runs locally | Google/Microsoft authorization **4** |
| COMMAND-01 / REPORT-01 | `/command`, `/agents`, `/agents/[id]`, `/command/reporting`; scoped leadership | Real event date filtering, explicit unknowns, drill-through, export, assignment evidence, training/transaction inspection | **1/3** agent-inspector tests3, scope and report tests, seven-role rendered review | Actual historical FUB coverage **4**; explicit held/created appointment evidence, stage intervals, received-cohort closing conversion and archived source history implemented; unknown source coverage remains explicit |
| TX-01 / DOC-01 / AI-01 / FUB-01 | Transactions, assistant, MCP, FUB operations | See `transactions-ai-build-register.md` for resource ownership, adapters, durable jobs, exact approvals, uploads, fixtures | **1/3** actual TC inspection→approval→outbox browser; stream protocol doubles; signed webhook fixture→canonical CRM | Real signing/PDF engine, allowed model, isolated Hermes worker, production FUB **4**; OS-contained per-owner lifecycle manager implemented; OS probe failed SIGABRT and no selected local model exists, so actual Hermes execution remains blocked **4**; separate authenticated local schedule worker verified automatic execution with zero external messages |
| MKT-01 | `/marketing`, `/approvals`; agent drafts, scoped reviewers | Eleven deterministic workflows; durable versions, tags, exact approval invalidation, local schedule outbox; private versioned PNG/JPEG/PDF/MP4 assets and downloads | **1/3** uploaded exact file survives refresh; unrelated owner404; schedule idempotency tests. Editorial calendar pause/cancel, private asset provenance/search, content previews/downloads and persisted history implemented; linked asset upload, approval invalidation, refresh and private delivery passed in the13 cross-module browser checks. Batch creation/pause/cancel and office isolation passed11 API checks. Related content asset IDs persist and invalidate prior approval | Actual channel permissions, rights, review, explicit publishing authorization **4** |
| EDU-01 / COM-01 | `/training`, classroom/course/lesson, community, manage; learners/scoped trainers | Actual curriculum14 courses181 lessons; private authorized delivery, progress, bookmarks, resources, actual videos; authored revisions/assignments/moderation | **1/3** see `academy-build-register.md`; real playback/range206, anonymous401, refresh progress | Missing unprovided brokerage recordings/remaining video production **4** |
| REC-01 | `/recruiting`, detail; scoped recruiters/owner | Confidential prospect CRUD, optimistic versions, notes, recorded calls, tasks, local calendar meetings, Joined→local invitation | **1/3** browser confidential history refresh and agent403. local invite/recovery/onboarding browser journey passes; confidential action history and Joined invitation use canonical access service | Real communications **4**; identity-consented learning linkage and immediate revocation verified; preview events remain browser-reported evidence |
| SET-01 | `/settings/personal`, account, brokerage, people, leads, transactions, ai, integrations, marketing, academy, website, audit | Each group routes to implemented settings owner; preferences and policy drafts; local record/API enforcement | **1/1** typecheck passed after CloudAI settings exports fixed; all routes tested | Auth/provider/engine-dependent capabilities **4** |
| DATA-01 | Canonical local repository, historical Postgres policies | SQLite transaction/rollback/optimistic enforcement; original three PostgreSQL migrations executed in isolated PGlite with least-privilege role | **1**8 real SQL policy checks; **5** canonical new role/schema PostgreSQL parity not established | Native server unavailable; no production DB approved |
| QA-01 | All roles/public families | Unit, typecheck, lint, production, browser, private asset checks, independent source review |538 tests passed (Sep 10); typecheck passed; production build passed; lint0 errors/26 warnings; dependency audit0 vulnerabilities.20 Axe page/theme checks on the final production app had0 violations;83 responsive/theme/keyboard/reduced-motion checks pass/no JS errors. Production mobile Lighthouse3 runs/page: median Performance97/97/98 for home/financing/buying; Accessibility/Best Practices/SEO100 each | Lab results, not field Core Web Vitals. Native browser zoom not verified;720px reflow simulation is explicitly labeled |


## Verification limits and activation boundaries

- Canonical local data uses SQLite plus server-side resource authorization. Eight least-privilege SQL policy checks executed on the original PostgreSQL migrations in PGlite. Canonical new schema/role PostgreSQL parity is **not established**; native PostgreSQL activation and its full policy verification remain required before production.
- Actual model/Hermes inference is blocked: no permitted local model is available and the installed Python runtime aborted under the OS isolation probe. Lifecycle ownership/stop behavior passed5 checks with an actual synthetic Node process. This is not Hermes inference proof. No paid fallback or shared Hermes runtime was invoked.
- Actual cryptographic signing/Documenso callbacks and external PDF/OCR services remain unavailable. Local PDF page inspection, unsigned placement previews, exact-version preparation and review are implemented and browser verified.
- No real account/sandbox connections or production effects were authorized. FUB/IDX/calendar/mail/social/ad activation require the named credentials, scopes, content rights and explicit authorization. Source gaps and factual/legal approval items remain in the public admin review queue.
- Controlled non-destructive backup/restore-copy and restart evidence is verified. In-place destructive reset/restore was not executed. Native browser zoom and field Core Web Vitals are not claimed.

## Latest evidence

- `verification/restart-results.json`: same opaque session;26 contacts,7 tasks including1 completed, personal preferences and8 recruiting records preserved by exact response hashes across dev→production restart.
- `verification/final-backup-results.json`: current SQLite hash and41 mutable file hashes verified; `runtime/backups/platform-1788916762755.sqlite` with paired `.files` bundle.
- `runtime/browser-evidence/completion-results.json`:13 cross-module checks; `access-policy-results.json`:8 invitation/recovery/settings checks; `viewport-results.json`:83 layout/theme/focus/reduced-motion checks.
- `runtime/pdf-preparation-evidence/result.json`: real2-page PDF inspection, unsigned preview, exact review, invalid-page and unauthorized denial; no page errors.
- `.runtime/public-source/cms-edge-verification.json`, `cms-v2-verification.json`, `cms-verification.json`: staged/published content, profile projection, actual308, noIndex/sitemap, metadata preview and archive regression checks (overlapping suites, not additive totals).
- `verification/indexing-results.json`:7 indexing/draft/authorization checks. `verification/hermes-lifecycle-results.json`:5 owned-process checks. `runtime/logs/final-unit.txt`, `production-build.txt`, `lint.txt`, `npm-audit-after.json`: final code gates.
- `academy-build-register.md`, `transactions-ai-build-register.md`, `.runtime/public-source/inventory.json` contain detailed source/page/workflow inventories.

## September 10 session

- Fixed `StoredAIConfig` duplicate export (`export export`) in `src/lib/services/cloud-ai/settings.ts`
- Fixed `AISettingsClient.tsx` imports: changed from `@/lib/services/cloud-ai/settings` to `@/lib/services/cloud-ai/shared-types` for `FullAIConfig`, `ValidationResult`, and `buildCloudConfig`
- Verified typecheck passes with zero errors
- Verified 538 tests pass
- Verified production build completes successfully
- Verified app runs on loopback:3200 with all routes returning expected status codes
- Created new backup: `runtime/backups/platform-1789072516246.sqlite` with paired `.files` bundle

## Storage integrity

All controlled work in this execution is directed inside the existing RCRE root. Initial source inventory: `verification/initial-integrity.json`; Academy source hashes: `academy-source-integrity.json`. Known exception: macOS created `/Users/jeremymcdonald/Library/Logs/DiagnosticReports/Google Chrome-2026-09-08-185040.ips` after an isolated browser crash. It was read only, not removed or modified. Earlier initial launch commands did not explicitly set npm cache paths, so this report does not claim proof of zero historical outside writes. No shared Hermes, launch configuration, or original training assets were intentionally modified.

Additional known OS-created storage exceptions during the Hermes isolation probe: `/Users/jeremymcdonald/Library/Logs/DiagnosticReports/python3.12-2026-09-08-205939.ips` and `python3.12-2026-09-08-205954.ips`. Read-only inspection; no outside repair/removal. Other pre-existing/concurrent Chrome diagnostics were observed but not attributed without evidence.

## Public elevation, metro journals and anonymous chat

Home market explorer/planner, three regional blog hubs, six new guides and unified18-story search are implemented. URL filters, regional links, image credits, source-backed geography and CMS published overrides integrated. Public chat is durable visitor-scoped guide mode with genuine optional local-model adapter, no private CRM/tools/paid fallback. Keyboard/modal/log announcement and delayed-history race fixes included. Current evidence and activation boundaries: `08-mvp/PUBLIC-ELEVATION-REVIEW.md`; production browser evidence `runtime/browser-evidence/metro-chat/results.json`.
