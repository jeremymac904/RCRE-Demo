{
  "name": "rcre-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Controlled MCP boundary between Hermes and RCRE. Narrow tools only.",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "devDependencies": { "typescript": "^5.7.3", "vitest": "^3.0.5", "@types/node": "^22.10.5" }
}
